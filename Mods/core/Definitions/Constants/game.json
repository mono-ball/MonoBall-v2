{
  "id": "base:constants:game",
  "constants": {
    "TileChunkSize": 16,
    "TileWidth": 16,
    "TileHeight": 16,
    "InputBufferTimeoutSeconds": 0.2,
    "InputBufferMaxSize": 5
  },
  "validationRules": {
    "TileChunkSize": {
      "min": 1,
      "description": "Tile chunk size must be positive"
    },
    "TileWidth": {
      "min": 1,
      "max": 128,
      "description": "Tile width must be between 1 and 128 pixels"
    },
    "TileHeight": {
      "min": 1,
      "max": 128,
      "description": "Tile height must be between 1 and 128 pixels"
    },
    "InputBufferTimeoutSeconds": {
      "min": 0.0,
      "max": 5.0,
      "description": "Input buffer timeout must be between 0 and 5 seconds"
    },
    "InputBufferMaxSize": {
      "min": 1,
      "max": 50,
      "description": "Input buffer size must be between 1 and 50"
    }
  }
}


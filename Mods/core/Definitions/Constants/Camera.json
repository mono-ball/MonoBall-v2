{
  "id": "base:constants:camera",
  "constants": {
    "ReferenceWidth": 240,
    "ReferenceHeight": 160,
    "CameraZoom": 1.0,
    "CameraRotation": 0.0,
    "CameraSmoothingSpeed": 0.1
  },
  "validationRules": {
    "ReferenceWidth": {
      "min": 1,
      "max": 4096,
      "description": "Reference width must be between 1 and 4096 pixels"
    },
    "ReferenceHeight": {
      "min": 1,
      "max": 4096,
      "description": "Reference height must be between 1 and 4096 pixels"
    },
    "CameraZoom": {
      "min": 0.1,
      "max": 10.0,
      "description": "Camera zoom must be between 0.1 and 10.0"
    },
    "CameraRotation": {
      "min": -6.28318,
      "max": 6.28318,
      "description": "Camera rotation must be between -2π and 2π radians"
    },
    "CameraSmoothingSpeed": {
      "min": 0.0,
      "max": 1.0,
      "description": "Camera smoothing speed must be between 0.0 (instant) and 1.0 (very smooth)"
    }
  }
}

